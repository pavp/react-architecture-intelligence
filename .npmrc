# Prevent pnpm from prompting to purge modules in non-TTY environments
# (e.g. goreleaser subprocess context, CI without TTY).
# pnpm v11 runs a deps-status-check before any script; if it detects a
# discrepancy it tries to re-install, but without a TTY it aborts.
confirm-modules-purge=false
