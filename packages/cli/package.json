{
  "name": "@rai/cli",
  "version": "0.0.0",
  "type": "module",
  "bin": { "rai": "./dist/index.js" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": { "@rai/core": "workspace:*", "@rai/adapter-next": "workspace:*", "@rai/adapter-react": "workspace:*" }
}
